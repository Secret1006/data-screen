#!/bin/bash
# 主部署脚本(调用docker网络自动配置脚本+数据大屏应用部署脚本)

# 执行顺序控制
set -e  # 遇到错误立即退出

# 获取当前脚本所在目录的绝对路径
SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)

echo "🚀 开始部署数据大屏系统"
echo "----------------------------------------------------------------"

# 1. 网络自动化配置
"$SCRIPT_DIR/docker_net_auto_config.sh"

echo "----------------------------------------------------------------"

# 2. 数据大屏应用部署
"$SCRIPT_DIR/deploy_app.sh"

echo "----------------------------------------------------------------"

echo "✅ 所有部署步骤已完成！！！"